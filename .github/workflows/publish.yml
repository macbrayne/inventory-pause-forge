# Builds and published to both CurseForge and Modrinth

name: Publish
# On releases (or manual)
on:
  workflow_dispatch:
  release:
    types: [published]

jobs:
  publish:

    runs-on: ubuntu-latest

    env: # Or as an environment variable
      MODRINTH: ${{ secrets.MODRINTH }}
      CURSEFORGE: ${{ secrets.CURSEFORGE }}
      CHANGELOG: ${{ github.event.release.body }}

    steps:
      - name: ✨ Checkout repository
        uses: actions/checkout@v4

      - name: 🏗 Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: 21
          distribution: adopt

      - name: 🤗 Setup Gradle, build and publish artifacts
        uses: gradle/actions/setup-gradle@v3

      - name: 🛠️ Publish with Gradle
        run: ./gradlew :fabric:runDatagen :neoforge:runData build modrinth curseforge

      - name: 📦 Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: artifacts
          path: '**/build/libs/'